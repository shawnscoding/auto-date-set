import React from "react";

const Header = () => {
  return (
    <React.Fragment>
      <h1>This is where magic happens!</h1>
    </React.Fragment>
  );
};

Header.propTypes = {};

export default Header;
